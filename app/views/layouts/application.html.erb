<!DOCTYPE html>
<html>
  <head>
    <title>RailsExercise</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <% if user_signed_in? %>
      <p class="nav user">
        Добро пожаловать, <b><%= current_user.email %></b> |
        <%= link_to "Logout", destroy_user_session_path, data: { "turbo-method": :delete } %></p>
    <%end%>

    <%= render "shared/flash" %>
    <%= yield %>
    <%= render "shared/footer" %>
  </body>
</html>
