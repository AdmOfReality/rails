<% if user_signed_in? %>
  <p class="nav user"> <%= t '.welcome' %>, <b><%= current_user.email %></b></p>
  <p class="nav user"><%= link_to "Logout", destroy_user_session_path, data: { "turbo-method": :delete } %></p>
<% end %>