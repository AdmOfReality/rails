<li><p>
	<%= answer.body %><br>
	<%= button_to 'Show', admin_answer_path(answer), :method => :get %>
    <%= button_to 'Edit', edit_admin_answer_path(answer), :method => :get %>
    <%= button_to 'Delete', admin_answer_path(answer), :method => :delete,
    		data: { turbo_confirm: 'Are you sure O_o ???'} %> </p>
</li>