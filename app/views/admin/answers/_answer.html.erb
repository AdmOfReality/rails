<li><p>
	<%= answer.body %><br>
	<%= button_to t('global_buttons.show'), admin_answer_path(answer), :method => :get %>
    <%= button_to t('global_buttons.edit'), edit_admin_answer_path(answer), :method => :get %>
    <%= button_to t('global_buttons.delete'), admin_answer_path(answer), :method => :delete,
    		data: { turbo_confirm: t('global_buttons.warning')} %> </p>
</li>