<%= form_with model: [:admin, @test, @question] do |form| %>
 
 <%= render 'shared/errors', resource: @question %> 
 
  <p>
    <%= form.label :body %><br>
    <%= form.text_field :body %>
  </p>

  <p><%= form.submit %></p>

<% end %>

<p><%= button_to t('global_buttons.back'), admin_tests_path(@question.test), :method => :get %></p>
