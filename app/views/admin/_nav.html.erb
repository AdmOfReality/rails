<nav class="navbar bg-primary" data-bs-theme="dark">
  <span>
    <%= link_to t('.header'), admin_tests_path, class: 'navbar-brand' %>
    <%= link_to t('.tests'), root_path, class: 'text-white' %> |
    <%= link_to 'Gists', admin_gists_path, class: 'text-white' %>
  </span>
  <span class="navbar-text text-white">
    <%= t('.welcome') %>, <b><%= current_user.email %></b> |
    <%= link_to t('global_buttons.logout'), destroy_user_session_path, data: { turbo_method: :delete } %>
  </span>
</nav>
